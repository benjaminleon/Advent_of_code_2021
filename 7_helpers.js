export function done(d) {
    return Object.keys(d).length == 1

    // let nr_of_occupied = 0
    // Object.entries(d).forEach(item => {
        // nr_of_occupied += item[1] > 0? 1: 0})
    // return nr_of_occupied == 1? true : false
}

